{
  "watch": ["src"],
  "ext": "ts,json",
  "ignore": ["src/**/*.test.ts", "dist/**"],
  "exec": "ts-node --transpile-only -r tsconfig-paths/register -r dotenv/config src/server.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
